import { MinutosSegundosPipe } from './minutos-segundos.pipe';

describe('MinutosSegundosPipe', () => {
  it('create an instance', () => {
    const pipe = new MinutosSegundosPipe();
    expect(pipe).toBeTruthy();
  });
});
